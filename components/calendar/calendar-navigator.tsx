import { Text } from '@/components/ui/text';
import { View } from 'react-native';

const CalendarNavigator = () => {
  return (
    <View className="mb-4 w-full border-b border-b-stone-300 bg-background p-4 dark:border-b-black">
      <Text>This will render the calendar navigator</Text>
    </View>
  );
};

export default CalendarNavigator;
